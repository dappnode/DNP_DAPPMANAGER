{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    /* Modules */
    "types": ["node", "mocha"],

    /* Emit */
    "outDir": "dist",

    /* Language and Environment */
    "lib": ["DOM", "ESNext"]
  },

  "include": ["src/**/*", "src/**/*.json"],
  "exclude": ["node_modules", "test/**/*", "dist"]
}
