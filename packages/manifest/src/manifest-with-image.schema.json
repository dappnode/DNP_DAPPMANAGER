{
  "title": "manifest",
  "type": "object",
  "required": ["name", "version", "image"],
  "properties": {
    "name": { "type": "string" },
    "version": { "type": "string" },
    "image": {
      "type": "object",
      "required": ["hash"],
      "properties": {
        "hash": { "type": "string" }
      }
    }
  }
}
