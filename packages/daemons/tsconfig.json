{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    /* Modules */
    "types": ["node", "mocha"],

    /* Emit */
    "outDir": "dist",

    /* Language and Environment */
    "lib": ["ESNext", "DOM"]
  },

  "include": ["src/**/*", "src/**/*.json"],
  "exclude": ["node_modules", "test", "dist"]
}
