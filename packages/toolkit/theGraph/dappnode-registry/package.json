{
  "name": "dappnode-registry",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dappnode-registry",
    "create-local": "graph create --node http://localhost:8020/ dappnode-registry",
    "remove-local": "graph remove --node http://localhost:8020/ dappnode-registry",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dappnode-registry",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.46.1",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "@graphprotocol/client-cli": "^2.2.22",
    "matchstick-as": "0.5.0"
  }
}
