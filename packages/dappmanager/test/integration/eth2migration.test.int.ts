import "mocha";
import { expect } from "chai";

describe("eth2migrations", () => {
  // Before do requirements:
  // - Setup web3signer container
  // - Setup validator container
  // - Setup validator volume

  before(async () => {
    // Setup validator container
    // Import keystores
    // Import slashing protection
    // Setup web3signer container
  });
});
