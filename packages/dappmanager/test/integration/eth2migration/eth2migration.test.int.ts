import "mocha";
import { expect } from "chai";

describe.only("eth2migrations", function () {
  before(async () => {
    // Connect to IPFS node for:
    // - web3signer installation package
    // - eth2client installation package
  });

  before(async () => {
    // Setup validator container
    // Import keystores
    // Import slashing protection
  });
});
