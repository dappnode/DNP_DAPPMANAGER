import { ZkevmConfigGet, InstalledPackageDetailData, ZkevmConfigSet } from "@dappnode/types";
import { dappnodeInstaller } from "../index.js";
//import { getZkevmConfig, setZkevmConfig } from "@dappnode/zkevm";

/* export async function zkevmConfigSet({
    rollup
  }: ZkevmConfigSet): Promise<void> {
    await setZkevmConfig(dappnodeInstaller, {
      rollup
    });
  } */

/**
 * ZKEVM configuration
 */

/* export async function zkevmConfigGet(): Promise<ZkevmConfigGet> {
  return await getZkevmConfig(dappnodeInstaller);
} */


