{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    /* Modules */
    "types": ["node", "mocha"],

    /* Emit */
    "outDir": "dist",
    "rootDir": "src",

    /* Language and Environment */
    "lib": ["ESNext"]
  },

  "include": ["src/**/*", "src/**/*.json"],
  "exclude": ["node_modules", "test/**/*", "dist"]
}
