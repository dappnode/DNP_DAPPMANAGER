{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    /* Modules */
    "types": ["node", "mocha"],

    /* Emit */
    "outDir": "dist",

    /* Language and Environment */
    "lib": ["ESNext", "DOM"]
  },

  "include": ["src/**/*", "src/**/*.json", "src/**/*.png"],
  "exclude": ["node_modules", "test/**/*", "dist"]
}
