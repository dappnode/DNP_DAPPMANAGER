{
  "name": "@dappnode/dappnode",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "repository": "git@github.com:dappnode/DNP_DAPPMANAGER.git",
  "license": "GPL-3.0",
  "engines": {
    "node": "22.x"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "yarn workspaces foreach --all -pt run build",
    "test": "yarn workspaces foreach --all -pt run test",
    "test:int": "yarn workspaces foreach --all -pt run test:int",
    "dev": "yarn workspaces foreach --all -pi run dev",
    "lint": "yarn workspaces foreach --all -pt run lint",
    "mock-standalone:build": "yarn workspaces focus admin-ui run mock-standalone:build",
    "generate": "yarn workspaces focus common run generate",
    "clean": "yarn workspaces foreach --all -pt run clean",
    "clean:libraries": "yarn workspaces foreach --all -pt run clean:libraries",
    "clean:build": "yarn workspaces foreach --all -pt run clean:build"
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.14.10",
    "@typescript-eslint/eslint-plugin": "^5.61.0",
    "@typescript-eslint/parser": "^5.61.0",
    "chai": "^4.3.10",
    "depcheck": "^1.4.7",
    "eslint": "^8.44.0",
    "eslint-config-react-app": "^7.0.1",
    "mocha": "^10.2.0",
    "nodemon": "^3.0.2",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "packageManager": "yarn@4.4.0"
}
